#include<stdio.h>
/**
 * main - function to print string to stdout
 * puts()- funtion to print string to stdout
 * Return: return o
 */
int main (void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
