#include<stdio.h>
/**
 * main - prints to stdout
 * printf - function that prints to stdout
 * Return: returns 0 success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
