#include <stdio.h>
#include "main.h"
/**
 * add - finds the sum of two integers
 * @a: parameter to be added
 * @b: parameter to be added
 * Return: return sum
 */
int add(int a, int b)
{
	int sum = 0;

	sum = a + b;
	return (sum);
}
